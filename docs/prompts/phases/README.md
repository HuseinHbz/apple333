# Approved phase prompts

Place one reviewed JSON prompt per future Apple333 phase in this directory.
Start from a file in `../templates/`, keep the file repository-relative, and
run `pnpm prompt:validate` before requesting implementation.

This directory intentionally contains no active phase prompt yet. Creating a
prompt describes and validates work; it does not authorize production access,
database activity, deployment, or an automatic commit.
